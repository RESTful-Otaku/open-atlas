/target
dist
node_modules
.DS_Store

# ---------------------------------------------------------------------------
# Local secrets & operator config — NEVER commit (see docs/CONFIG.md)
# ---------------------------------------------------------------------------
.env
.env.local
.env.capacitor.local
.env.*.local
!.env.example

# Feed API keys (copy docs/feed-secrets.example.json → .dev/feed-secrets.json)
**/feed-secrets.json
!docs/feed-secrets.example.json

# Local AI agent configs (opencode, Claude, Cursor, Copilot — NOT project code)
.opencode.md
AGENTS.md
CLAUDE.md
rules/
skills/
reports/

# Local dev / toolchain caches (dev.sh, CI agents — do not publish)
.dev/
.cargo-local/
.rustup-local/
