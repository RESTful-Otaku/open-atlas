// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 2.1.0 (commit 6981f48b4bc1a71c8dd9bdfe5a2c343f6370243d).

/* eslint-disable */
/* tslint:disable */
import {
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  procedures as __procedures,
  reducerSchema as __reducerSchema,
  reducers as __reducers,
  schema as __schema,
  t as __t,
  table as __table,
  type DbConnectionConfig as __DbConnectionConfig,
  type EventContextInterface as __EventContextInterface,
  type Infer as __Infer,
  type RemoteModule as __RemoteModule,
} from "spacetimedb";

import IngestEventReducer from "./ingest_event_reducer";
import LinkCausalEventsReducer from "./link_causal_events_reducer";

import CausalEdgeRow from "./causal_edge_table";
import DomainInsightRow from "./domain_insight_table";
import EventRow from "./event_table";
import EventNarrativeRow from "./event_narrative_table";
import SignalRow from "./signal_table";
import WorldStateRow from "./world_state_table";




const tablesSchema = __schema({
  causal_edge: __table({
    name: 'causal_edge',
    indexes: [
      { accessor: 'id', name: 'causal_edge_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'causal_edge_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, CausalEdgeRow),
  domain_insight: __table({
    name: 'domain_insight',
    indexes: [
      { accessor: 'domain', name: 'domain_insight_domain_idx_btree', algorithm: 'btree', columns: [
        'domain',
      ] },
    ],
    constraints: [
      { name: 'domain_insight_domain_key', constraint: 'unique', columns: ['domain'] },
    ],
  }, DomainInsightRow),
  event: __table({
    name: 'event',
    indexes: [
      { accessor: 'id', name: 'event_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'event_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, EventRow),
  event_narrative: __table({
    name: 'event_narrative',
    indexes: [
      { accessor: 'event_id', name: 'event_narrative_event_id_idx_btree', algorithm: 'btree', columns: [
        'eventId',
      ] },
    ],
    constraints: [
      { name: 'event_narrative_event_id_key', constraint: 'unique', columns: ['eventId'] },
    ],
  }, EventNarrativeRow),
  signal: __table({
    name: 'signal',
    indexes: [
      { accessor: 'id', name: 'signal_id_idx_btree', algorithm: 'btree', columns: [
        'id',
      ] },
    ],
    constraints: [
      { name: 'signal_id_key', constraint: 'unique', columns: ['id'] },
    ],
  }, SignalRow),
  world_state: __table({
    name: 'world_state',
    indexes: [
      { accessor: 'domain', name: 'world_state_domain_idx_btree', algorithm: 'btree', columns: [
        'domain',
      ] },
    ],
    constraints: [
      { name: 'world_state_domain_key', constraint: 'unique', columns: ['domain'] },
    ],
  }, WorldStateRow),
});


const reducersSchema = __reducers(
  __reducerSchema("ingest_event", IngestEventReducer),
  __reducerSchema("link_causal_events", LinkCausalEventsReducer),
);


const proceduresSchema = __procedures(
);


const REMOTE_MODULE = {
  versionInfo: {
    cliVersion: "2.1.0" as const,
  },
  tables: tablesSchema.schemaType.tables,
  reducers: reducersSchema.reducersType.reducers,
  ...proceduresSchema,
} satisfies __RemoteModule<
  typeof tablesSchema.schemaType,
  typeof reducersSchema.reducersType,
  typeof proceduresSchema
>;

/** The context type returned in callbacks for all possible events. */
export type EventContext = __EventContextInterface<typeof REMOTE_MODULE>;

/** Builder class to configure a new subscription to the remote SpacetimeDB instance. */
class SubscriptionBuilder extends __SubscriptionBuilderImpl<typeof REMOTE_MODULE> {}

/** Builder class to configure a new database connection to the remote SpacetimeDB instance. */
class DbConnectionBuilder extends __DbConnectionBuilder<DbConnection> {}


export class DbConnection extends __DbConnectionImpl<typeof REMOTE_MODULE> {

  static builder = (): DbConnectionBuilder => {
    return new DbConnectionBuilder(REMOTE_MODULE, (config: __DbConnectionConfig<typeof REMOTE_MODULE>) => new DbConnection(config));
  };


  override subscriptionBuilder = (): SubscriptionBuilder => {
    return new SubscriptionBuilder(this);
  };
}

